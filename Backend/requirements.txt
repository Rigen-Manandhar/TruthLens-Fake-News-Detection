fastapi==0.128.0
uvicorn==0.40.0
python-multipart==0.0.29
pydantic==2.12.5
# CUDA wheels are required for GPU inference/LIME model calls on the local RTX GPU.
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.12.0+cu130
torchvision==0.27.0+cu130
transformers==4.57.3
peft==0.18.0
python-dotenv==1.2.1
lime==0.2.0.1
numpy==2.2.6
scikit-learn==1.7.2
httpx==0.28.1
trafilatura==2.0.0
timm==1.0.27
opencv-python==4.13.0.92
Pillow==12.1.0
# facenet-pytorch is optional; required only for face cropping in the deepfake endpoint.
# If omitted, deepfake predictions still work but use the full image instead of a cropped face.
facenet-pytorch>=2.6.0
